VITE_AWS_API_URL=http://localhost:5500
VITE_AWS_REGION=eu-west-3

# AWS Parameter Store UI Environment Variables
#
# VITE_AWS_API_URL - The URL of the API endpoint for the Parameter Store UI
# VITE_AWS_REGION - The AWS region for SSM Parameter Store operations
#
# For local development, use the defaults above.
# For production deployment:
# - Set VITE_AWS_API_URL to your deployed API endpoint
# - Set VITE_AWS_REGION to the region where your parameters are stored
#
# Note: The application requires AWS credentials with permissions to:
# - ssm:GetParameter(s)
# - ssm:PutParameter
# - ssm:DeleteParameter
# - ssm:DescribeParameters
# - ssm:GetParameterHistory
